# Intake: MLX Benchmark Ladder With OpenMM Controlled Parity

Generated by auto-office-hours on 2026-05-23
Active change: 2026-05-23-mlx-benchmark-ladder-openmm-controlled-parity
Status: APPROVED
Mode: Builder
Work scale: capability
Work shape: mixed: benchmark design, audit, parity

## Objective

Make sure we have more relevant benchmarks for `mlx_atomistic`, in a clear and organized way, choosing the correct set of items to run efficiently and accurately, then compare the same runs against OpenMM where appropriate.

## Broader Intent

Use benchmark evidence to choose the next `mlx_atomistic` performance work without mixing internal MLX probes, OpenMM production `ns/day`, LAMMPS reference rows, and blocked real-system rows as if they were the same kind of evidence.

## Shape Context

### Closure Target

The accepted direction is Approach B with Ladder + OpenMM: design an MLX-centered benchmark ladder first, then implement missing OpenMM controlled references for GBSA/OBC and TIP4P-Ew so more rows become same-workload comparisons instead of blocked rows.

Reference context:
- OpenMM has benchmark families including GBSA, RF, PME, ApoA1, AMOEBA, and Amber20 systems such as DHFR, Cellulose, and STMV.
- LAMMPS has benchmark families including LJ, Chain, EAM, Chute, and Rhodopsin/protein-style workloads.
- Small reusable systems such as alanine dipeptide implicit, PME water boxes, and DHFR-style fixtures can guide future parity choices when they fit existing MLX capability.

### Key Questions

- Which benchmark rows best represent `mlx_atomistic` work rather than generic reference-engine performance?
- Which rows are micro, controlled MD, feature physics, scaling, reference parity, or real-system stretch?
- Which rows can be compared to OpenMM now, after controlled OpenMM implementation, or only later?
- What benchmark output is sufficient to select a next optimization target?

## Constraints

- Use `uv run ...` for Python execution.
- Keep MLX benchmark product code under `src/mlx_atomistic/benchmarks/`.
- Keep OpenMM reference execution under `scripts/`.
- Do not add OpenMM or LAMMPS to runtime dependencies.
- Keep raw benchmark outputs under gitignored `results/`.
- Keep committed interpretation under `docs/benchmarks/`.
- Use normalized benchmark schema and concrete `blocked` reasons.

## Scope Coverage

- Included: MLX-centered benchmark ladder design.
- Included: organize benchmark rows by purpose and decision value.
- Included: comparison metadata and reporting structure for benchmark rows.
- Included: OpenMM controlled implementations for GBSA/OBC and TIP4P-Ew.
- Included: efficient runnable commands and tests proving normalized `ok` or `blocked` behavior.
- Deferred: LAMMPS first-class mapping unless it falls out cleanly from simple LJ-like workloads.
- Deferred: DHFR or other real-system MLX prepared-artifact/runtime path.
- Deferred: performance optimization itself; this change creates benchmark evidence, not optimizer changes.
- Anti-goals: no fake MLX-vs-OpenMM claims.
- Anti-goals: no mixing `steps/s`, `ms/eval`, and `ns/day` as equivalent.
- Anti-goals: no broad framework leaderboard.

## Scope Preservation

This preserves the user's full stated intent by treating benchmark design and OpenMM controlled parity as one coherent capability. It intentionally defers LAMMPS and DHFR real-system parity because those are separate execution risks.

## Recommended Approach

Approach B with Ladder + OpenMM: build the MLX-centered benchmark ladder and implement the missing OpenMM controlled GBSA/OBC and TIP4P-Ew references so the next report has more real same-workload rows.

## Key Assumptions and Risks

- OpenMM GBSA/OBC can be implemented as a small controlled OBC force evaluation without needing full production DHFR.
- OpenMM TIP4P-Ew can be implemented as a controlled virtual-site or tiny water reference, but it may be diagnostic rather than equivalent to full water dynamics unless operation semantics match MLX.
- Metal/OpenCL availability may require non-sandboxed local execution for measured rows.
- If OpenMM controlled cases reveal semantic mismatch, the correct outcome is `diagnostic` or `blocked`, not a forced ratio.

## Rejected or Deferred

- Minimal ladder only was rejected because it would leave the important OpenMM GBSA/OBC and TIP4P-Ew rows blocked.
- Full parity push was deferred because adding LAMMPS mapping and DHFR real-system path in the same spec would dilute the MLX benchmark design objective and add fixture/runtime risk.

## Next Steps

1. Frame a spec that defines the benchmark ladder, row taxonomy, and acceptance criteria.
2. Plan implementation slices for MLX metadata/reporting, OpenMM GBSA/OBC, OpenMM TIP4P-Ew, and comparison reporting.
3. Verify with focused tests, controlled smoke commands, and a human-readable benchmark report.
